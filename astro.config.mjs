import { defineConfig } from "astro/config";

// https://astro.build/config
export default defineConfig({
  site: "https://kyuen19.github.io",
  // If deploying to a repo other than username.github.io, set base:
  // base: '/your-repo-name',
});
